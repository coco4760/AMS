services:
  client-web-html:
    image: rd.clouditera.com/aigc/client-web:${CLIENT_WEB_HTML_VER}
    restart: always
    ports:
      - '80:80'
    extra_hosts:
      - "kong.host.com:${KONG_HOST}"
      - "kong:${KONG_HOST}"
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
