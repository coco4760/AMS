app:
  description: 对应RAG search的API接口
  icon: 🤖
  icon_background: '#FFEAD5'
  mode: workflow
  name: 【安全论文检索】论文元数据获取20250901
  use_icon_as_answer_icon: false
dependencies: []
kind: app
version: 0.2.0
workflow:
  conversation_variables: []
  environment_variables: []
  features:
    file_upload:
      allowed_file_extensions:
      - .JPG
      - .JPEG
      - .PNG
      - .GIF
      - .WEBP
      - .SVG
      allowed_file_types:
      - image
      allowed_file_upload_methods:
      - local_file
      - remote_url
      enabled: false
      fileUploadConfig:
        audio_file_size_limit: 50
        batch_count_limit: 5
        file_size_limit: 15
        image_file_size_limit: 10
        video_file_size_limit: 100
        workflow_file_upload_limit: 10
      image:
        enabled: false
        number_limits: 3
        transfer_methods:
        - local_file
        - remote_url
      number_limits: 3
    opening_statement: ''
    retriever_resource:
      enabled: true
    sensitive_word_avoidance:
      enabled: false
    speech_to_text:
      enabled: false
    suggested_questions: []
    suggested_questions_after_answer:
      enabled: false
    text_to_speech:
      enabled: false
      language: ''
      voice: ''
  graph:
    edges:
    - data:
        isInIteration: false
        isInLoop: false
        sourceType: start
        targetType: code
      id: 1750672741748-source-1756449798069-target
      source: '1750672741748'
      sourceHandle: source
      target: '1756449798069'
      targetHandle: target
      type: custom
      zIndex: 0
    - data:
        isInLoop: false
        sourceType: code
        targetType: end
      id: 1756449798069-source-17564504241350-target
      source: '1756449798069'
      sourceHandle: source
      target: '17564504241350'
      targetHandle: target
      type: custom
      zIndex: 0
    nodes:
    - data:
        desc: ''
        selected: false
        title: 开始
        type: start
        variables:
        - label: kb_id
          max_length: 48
          options: []
          required: true
          type: text-input
          variable: kb_id
        - label: doc_ids
          max_length: 50000
          options: []
          required: true
          type: paragraph
          variable: doc_ids
      height: 116
      id: '1750672741748'
      position:
        x: -29.75347606390403
        y: 282
      positionAbsolute:
        x: -29.75347606390403
        y: 282
      selected: true
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        code: "import requests\n\ndef main(arg1: str, arg2: str) -> dict:\n    KB_ID\
          \ = (arg1 or \"\").strip()\n    API_KEY = \"f03c5ea41a714aee97b8105f63a997fb\"\
          \n    BASE = \"https://rag.clouditera.com/api/v1/documents\"\n    headers\
          \ = {\"accept\": \"application/json\", \"X-API-Key\": API_KEY}\n\n    #\
          \ 解析 doc_ids：支持 \"a,b c\" 或 \"doc_ids=a&doc_ids=b\"\n    raw = (arg2 or\
          \ \"\").strip()\n    parts = []\n    for tok in raw.replace(\",\", \" \"\
          ).replace(\"&\", \" \").split():\n        if tok.startswith(\"doc_ids=\"\
          ):\n            tok = tok[8:]\n        if tok:  # 简单校验：只要非空就收\n        \
          \    parts.append(tok)\n\n    # 去重并保持顺序\n    seen = set()\n    ids = []\n\
          \    for x in parts:\n        if x not in seen:\n            seen.add(x)\n\
          \            ids.append(x)\n\n    # 分批（每批最多 50 个）\n    chunks = [ids[i:i+50]\
          \ for i in range(0, len(ids), 50)] if ids else []\n\n    all_items, errors\
          \ = [], []\n    for batch in chunks:\n        qs = \"&\".join(f\"doc_ids={i}\"\
          \ for i in batch)  # doc_id 为 hex，无需额外转义\n        url = f\"{BASE}/{KB_ID}/batch_detail?{qs}\"\
          \n        try:\n            r = requests.get(url, headers=headers, timeout=30)\n\
          \            if r.status_code == 200:\n                j = r.json()\n  \
          \              if isinstance(j, dict) and \"data\" in j and isinstance(j[\"\
          data\"], list):\n                    all_items.extend(j[\"data\"])\n   \
          \             else:\n                    errors.append({\"batch_size\":\
          \ len(batch), \"reason\": \"unexpected_response\"})\n            else:\n\
          \                errors.append({\"batch_size\": len(batch), \"status\":\
          \ r.status_code, \"text\": r.text[:200]})\n        except Exception as e:\n\
          \            errors.append({\"batch_size\": len(batch), \"error\": str(e)})\n\
          \n    # 结果按 doc_id 去重\n    dedup_seen, dedup = set(), []\n    for it in\
          \ all_items:\n        did = (it.get(\"doc_id\") or \"\").strip()\n     \
          \   if did and did not in dedup_seen:\n            dedup_seen.add(did)\n\
          \            dedup.append(it)\n\n    out = {\n        \"status\": \"success\"\
          \ if not errors else (\"partial_success\" if dedup else \"failed\"),\n \
          \       \"requested\": len(ids),\n        \"fetched\": len(dedup),\n   \
          \     \"batches\": len(chunks),\n        \"data\": dedup,\n        \"errors\"\
          : errors\n    }\n    return {\"result\": json.dumps(out, ensure_ascii=False)}"
        code_language: python3
        desc: ''
        outputs:
          result:
            children: null
            type: string
        selected: false
        title: 获取RAG数据
        type: code
        variables:
        - value_selector:
          - '1750672741748'
          - kb_id
          variable: arg1
        - value_selector:
          - '1750672741748'
          - doc_ids
          variable: arg2
      height: 54
      id: '1756449798069'
      position:
        x: 477.21337232279404
        y: 282
      positionAbsolute:
        x: 477.21337232279404
        y: 282
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        desc: ''
        outputs:
        - value_selector:
          - '1756449798069'
          - result
          variable: body
        selected: false
        title: 结束 (1)
        type: end
      height: 90
      id: '17564504241350'
      position:
        x: 884.1659625443332
        y: 282
      positionAbsolute:
        x: 884.1659625443332
        y: 282
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    viewport:
      x: 313.38533640970024
      y: 78.37126758034947
      zoom: 0.8304095939346113
