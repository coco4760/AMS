services:
  llm_sast_backend:
    image: rd.clouditera.com/aigc/llm_sast:x86_64-0.3.7-backend
    ports:
      - "${DEFECT_JUDGEMENT_BACKEND_PORT:-20013}:8081"
    volumes:
      - ./data/backend:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - POSTGRES_SERVER=postgres
      - UPLOAD_DIR=/app/data/uploads
      - CACHE_DIR=/app/data/cache
    env_file:
      - .env
    depends_on:
      - postgres
    restart: always
    networks:
      - llm-sast-network

  llm_sast_frontend:
    image: rd.clouditera.com/aigc/llm_sast:x86_64-0.3.6-frontend
    ports:
      - "${DEFECT_JUDGEMENT_FRONTEND_PORT:-20014}:8080"
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - POSTGRES_SERVER=postgres
      - UPLOAD_DIR=/app/data/uploads
    env_file:
      - .env
    depends_on:
      - postgres
    restart: always
    networks:
      - llm-sast-network

  postgres:
    image: rd.clouditera.com/docker/pgvector:pg17
    # 内部服务，不对外暴露端口
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - .env
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=llm_sast
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    restart: always
    networks:
      - llm-sast-network

volumes:
  postgres-data:

networks:
  llm-sast-network:
    driver: bridge
