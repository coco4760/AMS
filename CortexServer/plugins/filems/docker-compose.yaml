services:
  filems:
    image: rd.clouditera.com/infra/filems:sast-v2.5-0
    restart: always
    logging:
      options:
        max-size: "5G"
    volumes:
      - ./config/config.json:/root/config.json
    ports:
      - "${FILEMS_PORT_1:-20022}:8840"
      - "${FILEMS_PORT_2:-20023}:2201"
