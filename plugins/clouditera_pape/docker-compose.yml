services:
  paper-app:
    image: rd.clouditera.com/clouditera_knowledage/clouditera_knowledage_paper_api:1.0
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - ./paper_storage:/app/paper_storage
    command: >
      sh -c "uvicorn main:app --host 0.0.0.0 --port 8000"
    restart: unless-stopped
