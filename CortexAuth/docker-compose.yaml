services:
  auth-http-api:
    image: rd.clouditera.com/aigc/wuji/auth-http-api:v1.5.14
    restart: always
    environment:
      CONTEXT_PATH: /authapi
    env_file:
      - .env
    ports:
      - "${AUTH_HTTP_PORT:-20001}:80"
    volumes:
      - ${INSTALL_LOCAL}/wuji/volumes/webide/code:/tmp/webide
      - ./config/application.yml:/app/resources/application.yml
